# دليل نشر موقع النسيم خطوة بخطوة

الموقع مبني بـ **Next.js** ومتصل بقاعدة بيانات **Supabase** (مجانية) حتى العميل يقدر يضيف/يعدل/يحذف الأصناف من لوحة تحكم بدون ما يلمس الكود.

---

## 1) إنشاء مشروع Supabase

1. روح لـ [supabase.com](https://supabase.com) وسوي حساب مجاني (تقدر تسجل بـ GitHub).
2. اضغط **New Project**.
3. اختار اسم للمشروع (مثلاً `nassim-store`)، وكلمة سر لقاعدة البيانات (احفظها بمكان آمن)، والمنطقة الأقرب إلك.
4. استنى شوي (حوالي دقيقة) لحد ما المشروع يجهز.

## 2) تجهيز الجداول

1. من القائمة الجانبية، افتح **SQL Editor**.
2. افتح ملف `supabase/schema.sql` الموجود بالمشروع، انسخ كل محتواه، والصقه بالمحرر.
3. اضغط **Run**. هيك رح تتعمل جداول الأصناف (`products`) والرسائل (`messages`) مع صلاحيات الحماية (RLS).

## 3) تجهيز تخزين الصور

1. من القائمة الجانبية افتح **Storage**.
2. اضغط **New bucket**، سمّيه بالظبط: `product-images`.
3. فعّل خيار **Public bucket** (حتى الصور تظهر بالموقع للزوار).
4. رجع لـ **SQL Editor** — الصلاحيات الخاصة برفع الصور موجودة بنفس ملف `schema.sql` وتكون انعملت مع الخطوة السابقة تلقائياً.

## 4) إنشاء حساب الأدمن (العميل)

1. من القائمة الجانبية افتح **Authentication → Users**.
2. اضغط **Add user → Create new user**.
3. حط إيميل وكلمة سر للعميل (هاد الحساب يلي رح يسجل فيه دخول للوحة التحكم).
4. تأكد الخيار **Auto Confirm User** مفعّل حتى ما يحتاج تفعيل بريد.

## 5) الحصول على مفاتيح الاتصال

1. من القائمة الجانبية افتح **Settings → API**.
2. انسخ:
   - **Project URL**
   - **anon public key**

## 6) تشغيل المشروع على جهازك (اختياري، للتجربة قبل النشر)

1. فك ضغط ملف المشروع، وافتح تيرمينال جواه.
2. سوي نسخة من `.env.local.example` باسم `.env.local` وحط فيه القيم يلي نسختها بالخطوة السابقة:
   ```
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   ```
3. نفذ:
   ```
   npm install
   npm run dev
   ```
4. افتح المتصفح على `http://localhost:3000` — لازم يطلع الموقع شغال، و `http://localhost:3000/admin/login` تسجل فيها دخول بحساب الأدمن يلي سويته.

## 7) رفع المشروع على GitHub

1. سوي repository جديد فاضي على [github.com](https://github.com).
2. من تيرمينال المشروع:
   ```
   git init
   git add .
   git commit -m "نسخة أولى من موقع النسيم"
   git branch -M main
   git remote add origin <رابط الريبو تبعك>
   git push -u origin main
   ```

## 8) النشر على Vercel (مجاني)

1. روح لـ [vercel.com](https://vercel.com) وسجل دخول بحساب GitHub.
2. اضغط **Add New → Project** واختار الريبو يلي رفعته.
3. بصفحة الإعدادات، افتح **Environment Variables** وضيف:
   - `NEXT_PUBLIC_SUPABASE_URL` = رابط مشروعك بـ Supabase
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = المفتاح العام
4. اضغط **Deploy** واستنى دقيقة لحد ما يخلص.
5. رح ياخدك Vercel لرابط الموقع الجاهز (مثلاً `nassim-store.vercel.app`) — هاد هو رابط الموقع النهائي.

## 9) استخدام لوحة التحكم

- رابط لوحة التحكم: `<رابط موقعك>/admin/login`
- سجل دخول بإيميل وكلمة السر يلي سويتهم بالخطوة 4.
- من تبويب **الأصناف**: ضيف/عدّل/احذف أي صنف مع صورته وسعره وحالة التوفر.
- من تبويب **الرسائل**: بتشوف أي رسالة يبعتها زبون من صفحة "تواصل معنا".

---

### ملاحظات

- لو بدك تغيّر رقم الهاتف أو العنوان الظاهرين بالموقع، هدول لسا مكتوبين مباشرة بالكود (بالفوتر وصفحة التواصل) — قلي وقتها أضيفهم كحقول قابلة للتعديل من لوحة التحكم كمان.
- في خطة مجانية عند Supabase وVercel كافية لموقع صغير متل هيك، بس فيها حدود استخدام — إذا الموقع كبر كتير ممكن تحتاج ترقية لاحقاً.
